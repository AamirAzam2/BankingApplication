# BankingApplication
The application provides secure APIs for user account management, fund transfers, transaction history, bank statement generation (PDF), and email notifications — all protected via JWT-based authentication.
